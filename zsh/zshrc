# --------------------------------
#
# DO NOT EDIT THIS FILE.
# If you want to make changes, do so in the custom/zsh directory.
# Fork and send pull requests to add features and fix bugs. Thanks!
#
# ================================

# Don't change. The following determines where YADR is installed.

function find_yadr() {
  yadr=`fdfind -H .yadr $HOME | head | sed 's/\.yadr//'`
  export yadr
}

autoload -U promptinit
promptinit
prompt adam1
find_yadr

# Configuration
for config_file ($yadr/zsh/*.zsh) source $config_file

# Put secret configuration settings in ~/.secrets
if [[ -a ~/.secrets ]] then
  source ~/.secrets
fi

# added by pipx (https://github.com/pipxproject/pipx)
export PATH="/root/.local/bin:$PATH"

#eval $(op signin petermorgan)

#source /root/.config/broot/launcher/bash/br
export PATH="$PATH:$HOME/.axiom/interact"
source /root/.axiom/functions/autocomplete.zsh
compdef _axiom-ssh axiom-rm
compdef _axiom-ssh axiom-ssh
compdef _axiom-ssh axiom-select
compdef _axiom-ssh axiom-backup
compdef _axiom-ssh axiom-vpn
compdef _axiom-restore axiom-restore
compdef _axiom-deploy axiom-deploy
